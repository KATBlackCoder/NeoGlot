// Formatter RPG Maker Classic (XP/VX/VXAce) — réutilise RpgMakerFormatter
// Les mêmes codes moteur (\C, \N, \V, \I, etc.) sont utilisés dans toutes les versions RPG Maker
pub use crate::commands::engines::rpgmv::formatter::RpgMakerFormatter as RpgMakerClassicFormatter;
