// Réinjection Wolf RPG — WolfTL réinjecte les traductions dans les fichiers .dat/.mps
// Implémenté en T09

#[tauri::command]
pub fn inject_wolf(_project_id: i64) -> Result<(), String> {
    Err("Non implémenté — voir T09".into())
}
