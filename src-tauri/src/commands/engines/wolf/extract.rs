// Extraction Wolf RPG — UberWolf (décryptage) puis WolfTL (extraction textes)
// Implémenté en T09

#[tauri::command]
pub fn extract_wolf(_project_id: i64) -> Result<usize, String> {
    Err("Non implémenté — voir T09".into())
}
